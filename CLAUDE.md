# 《中国人能飞》游戏项目备忘

单文件网页游戏：`index.html`（Canvas 2D + 原生 JS + CSS，无外部依赖）。

## 部署与登录（2026-08-25 更新）

- **线上地址**：https://dream1238.github.io/Chinese_can_fly_game/ （仓库 dream1238/Chinese_can_fly_game，公开）
- **登录流程**：未登录一律显示登录页；**无自动登录**（已移除 ccf_first_launch 自动登录 YH 逻辑）。
- **开发者账号 YH（密码 456789）**：ensureDevAccount 在**所有环境**（本地/线上）无条件预置；旧版本数据存在时自动导入（importMigratedTo 键名按 CONFIG.storage 映射）。
- **音频资源**：全部位于游戏目录 `audio/` 子目录（含 BGM×4、音效×7、揽佬头像.jpeg、外星人.png），`CONFIG.audio.desktopDir = 'audio/'` 相对路径。缺失文件（shoot/explosion/boss mention/click）走合成音效。
- **迁移键名 bug（已修）**：importMigratedTo 必须按 CONFIG.storage 映射回属性名写入 `ccf_u_<用户>_<属性名>`（旧实现拼成 `ccf_u_YH_ccf_redPackets` 永读不到）。
- **手机端**：竖屏显示全屏「建议横屏游玩」遮罩（#orient-overlay，resize 时按宽高比切换）；矮屏（max-height 430px）界面保持垂直居中+可滚动防溢出。
- **虚拟摇杆**（左下角 #joystick）：触摸设备局内用摇杆控制移动，归一化幅度（0..1）× 玩家速度，与键盘速度一致；**已替代旧触摸拖拽**（dragTarget 逻辑移除，S 保留遗留字段）。
- **段位晋升动画**：绘制在最顶层独立画布 #rank-anim-layer（z-index 90，全视口，背景透明可看到首页），彩带粒子存动画自有数组 a.shards；resize 同步「视口px→wrap px→960x540 逻辑坐标」变换。

## 核心概念定义（务必遵守）

- **「局 / 挑战」**：从选择进入关卡模式或无尽模式后开始，以**任何方式退出本次模式**为结束（失败 / 全部通关 / 暂停后回主菜单 / 重新开始 / 关闭页面等），算作一局 / 一次挑战。
- **「关」**：仅指关卡模式下的一关（第 1 / 2 / 3 关）。关卡模式下同一局挑战跨越全部 3 关，**关卡之间的通关过渡不算挑战结束**。

## 由概念衍生的装备机制（已按此实现）

- **紧急维修（一次性）**：一次挑战（模式全程）内被击毁时复活 1 次（+1 生命、无敌 3 秒）。
  - 关卡模式下**跨关有效**：第 1 关未触发，第 2 / 3 关仍生效。
  - 挑战结束（阵亡 / 全部通关 / 回主菜单 / 重新开始 / 关页面）时失效，需重新装备。
- **聚宝盆（一次性）**：下一次无尽模式挑战内红包掉落概率 88%，该次挑战结束（阵亡 / 回主菜单 / 重开 / 关页面）失效。
- **八方来财（永久）**：关卡模式全部通关（第 3 关结算）红包 +8（装备加成）。
- **大变幡（永久）**：无尽模式「变身」道具掉落概率 5% → 10%。
- **火力支援（永久）**：按 M 键召唤两个伴生僚机 10 秒（每秒锁定最近敌机发射追踪弹，伤害与主机本体相同，含 Boss 目标），召唤结束后冷却 8 秒；僚机从主机左右偏下渐显/渐隐，渐显/渐隐音效为自主合成（渐隐 = 渐显倒放），僚机发射音效独立合成；购买/使用音效为大展宏图；价格 300 红包，需上尉及以上段位。
- **领域（永久）：错乱时空**：每 12 秒 66% 概率进入时空静止 6 秒——所有敌机/障碍（含激光、外星人）/敌机子弹冻结，仅我方英雄正常移动攻击（玩家子弹与僚机正常）；状态期间播放桌面「时空暂停音效.m4a」（一次性 BGM，结束恢复本局 BGM），背景持续微抖 + 紫色错乱扫描条 + 时间涟漪特效，HUD 显示紫色「时空静止」进度条；购买/使用音效为大展宏图；价格 300 红包，需上尉及以上段位。静止时敌机生成/Boss/外星人刷新计时全部暂停。

## 商店原价（测试期已临时归零，等用户说"一键恢复原价"时恢复）

大变幡（永久）50 / 八方来财（永久）88 / 聚宝盆（一次性）30 / 紧急维修（一次性）30 / 黄金之翼 200 / 冷银之翼 200 / I can fly 350。原价清单另存于全局记忆 `game-shop-prices.md`。

## 留存功能（2026-08 新增）

- **段位称号**（条件化晋级，只取无尽最高分为分数标准）：见习机长 0 / 少尉 5000+通关第2关 / 中尉 20000+通关第3关 / 上尉 50000+关卡最高分≥30000 / 王牌机长 100000+击杀Boss 100次（两模式均计，键 ccf_boss_kills）/ 传奇机长 200000+三关无伤通关100次（showVictory 时 deaths===0 计数，键 ccf_perfect_clears）。主菜单「🏅 段位」按钮进入段位图鉴页面（分数进度条 + 任务进度条 + 6 档要求解释 + 达标状态）；结算界面按本局开局段位快照（rankStart）显示晋升提示。商店「I can fly」皮肤需中尉及以上段位才能购买。
- **飞行日志**：累计飞行局数（进入挑战 +1，按"局"定义）、累计击毁敌机数（含 Boss）、累计飞行时长；主菜单显示；`commitLogStats()` 在 toMenu/restartCurrent/beforeunload 落盘。
- **昨日对比**：无尽模式按"当日最高分"记录（键 dailyDay/dailyBest/dailyPrev），跨天惰性归档（`checkDayRollover`：今日→昨日、今日清零）；主菜单与无尽结算显示「今日 X / 昨日 Y」。
- **每日彩蛋·金飞机**：按日期种子（`mulberry32(hashStr(getDateStr()))`）确定性生成当天开局延迟(2-8s)与航道，同一天所有对局一致、每天不同；**当天严格仅出现一次**（键 goldShown 标记，出现后当天不再刷新；关卡仅第 1 关、无尽模式开局后出现，`nextLevel` 不重置 → 第 2/3 关不出）；击落 +800 分 + 红包（固定 +5，键 goldDay/goldFirst 跨天惰性重置），被撞击不给奖励。

## 其他要点

- 玩家受伤判定边界 = 中心圆形机舱（非机翼）。
- I can fly 高空飞行：每 10 秒 50% 概率进入 8 秒，无视敌机本体和障碍碰撞，碰到子弹仍受伤；期间切换专属 BGM。
- 无尽模式：Boss 每 20 秒刷新、MV里的外星人障碍每 10 秒从屏幕边缘刷新（越飞越快）、血槽 5 格。
- 浏览器自动播放策略会拦截有声自动播放：页面加载后自动尝试，被拦截时静音播放并在首次交互恢复声音；如需打开即有声请用户在浏览器设置允许该站点自动播放。
