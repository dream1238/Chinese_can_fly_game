# 《中国人能飞》开发反思报告

> 2026-08-29 · 音频问题三次修复失败后的总结（用户要求写入项目专门记忆文件）

## 一、事件回顾（BGM 问题的三次失败）

| 轮次 | 我的做法 | 结果 |
|---|---|---|
| 第 1 轮 | 发现线上无音频文件（路径指向桌面 file://）→ 上传 audio/ + 改相对路径；发现"查无此号" | 手机端 BGM 仍异常（"必须退出重登才是原版"） |
| 第 2 轮 | 判定 iOS 不预加载 → 加显式 load() + loadeddata + 12s 超时 + 三重自动重试 | 手机端启动 16MB 全量下载 → **变卡** |
| 第 3 轮 | 改按需加载（loadAll 只载音效+menu） | **引入新 bug：playBGM 按需 tryLoad 成功后缺 .then 接播放** → 桌面关卡 BGM 不响、手机更糟 |

## 二、根因反思（为什么同一个问题反复修不好）

1. **只打补丁、不验证根因**：第 2 轮直接改"加载机制"前，没有先确认"线上 URL 是否可达、桌面与移动端加载行为差异的完整链路"。直到用户投诉"变卡"才意识到 iOS 全量下载问题。
2. **没有分端思考**：桌面（preload=auto 自动下载，首次机制本就正常）与移动端（iOS 不显式 load 不下载）行为完全不同，我却用一套逻辑改两端，结果一端修好另一端坏。
3. **改动后没有完整回归**：第 3 轮按需加载改动后，playBGM 的新代码路径（按需 tryLoad 的 .then 接播放）**从未被验证**——沙箱测试覆盖了 loadAll 行为却没覆盖 playBGM 的按需分支 → 桌面关卡 BGM 静音的 bug 直接上线。
4. **没有"恢复能力"意识**：用户明确说"电脑端恢复首次版本最好"——首次上线版本（5a04fec）的音频机制（全量加载 + canplaythrough + 4s 超时）在桌面浏览器上本来就是正常的，我不该整体改掉。

## 三、本次修复的正确做法（已实施）

1. **分端架构**：`IS_TOUCH` 区分——桌面恢复首次机制（全量加载 + canplaythrough + 4s 超时，浏览器原生 preload=auto）；移动端保持修复机制（显式 load + loadeddata + 12s + 按需加载）。
2. **修复 .then 缺失 bug**：playBGM 按需 tryLoad 的 `.then` 接上 startLoadedBgm（此前加载完也不播）。
3. **独立进程分端沙箱验证**：desktop/mobile 两场景分别模拟（canplaythrough-only / loadeddata-only + 404 回退），桌面 7 项 + 手机 6 项全部通过后才允许上线。

## 三·补、最终根因（2026-08-30 线上实测确认）

用户提供关键信息（版本号正常 + 音频失败报告 18 个 + 本地 file:// 完美）后定位：

1. **"本地完美、线上全坏"的真相 = 4s 超时 + 死等 canplaythrough**：恢复首次版本机制时沿用 4s 超时 + canplaythrough（需下载完整个文件）——本地 file:// 磁盘秒载正常；**线上要网络下载 16MB，4 秒根本不够 → 全部超时误判缺失 → 全合成**。教训：**"恢复首次版本效果"必须适配线上网络条件**（loadeddata 数据可播即成功 + 15s 超时），不能原样照搬本地参数。
2. **"原版+合成同时播放" = startLoadedBgm 替换时没停掉合成**：重试成功后原版接入，但 SynthBGM 未停止且此后 stopBGM 只按 currentBgm==='synth' 停 → 合成长鸣。已修复（startLoadedBgm 先 SynthBGM.stop()）。
3. **排查工具（构建版本号 + 音频失败可视化报告）是本次破局关键**：让用户一次反馈就能区分"缓存/代码/加载失败"，避免再次盲猜。

## 四、长期改进措施（以后遇到问题务必遵守）

1. **先复现/验证根因，再动手**：改动前用沙箱或线上实测确认问题链路，禁止"猜测式修补"。
2. **改动必须配套验证**：任何代码路径的修改（尤其新增分支），沙箱测试必须覆盖该路径本身，不只是"不报错"。
3. **保留"恢复锚点"**：用户认可过的行为（如首次版本音频）作为基准锚点，改动时优先考虑"在锚点上最小化修改"，而非重写。
4. **多端差异必须分端处理**：桌面/移动端（iOS 自动播放与预加载策略差异）分别考虑，一套逻辑不适用两端时拆开。
5. **上线前自检清单**：语法 → 沙箱（含新增路径）→ 线上 URL 实测 → 分端行为 → 才允许推送。
