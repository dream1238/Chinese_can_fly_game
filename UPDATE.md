# 《中国人能飞》更新方案

## 一、更新发布流程（每次更新只需 3 步）

1. **修改代码**：编辑 `index.html`（单文件游戏，全部功能集中于此），本地验证通过。
2. **保存版本**：`git add -A && git commit -m "更新说明"`（本地版本记录）。
3. **发布上线**：`git push origin main` → GitHub Pages 自动构建生效（约 1-2 分钟）。

## 二、更新如何同步到所有已注册用户

架构说明：游戏是**纯前端静态部署**（GitHub Pages），每个用户的游玩数据保存在**各自浏览器的 localStorage** 中，与代码完全分离。

- **代码更新 = 全量同步**：推送后，所有已注册未注销的用户**下次打开网页即自动运行新版本**，无需任何手动操作。
- **用户数据零影响**：新版本不会触碰用户各自命名空间（`ccf_u_<用户名>_*`）的数据，旧数据完整保留。
- **新增功能键的兼容**：新增持久化键时一律通过 `storeGet(键, 默认值)` 读取——老用户自动获得默认值（如新道具默认未购买），无需迁移脚本。

## 三、需要特殊处理的情况

| 场景 | 处理方式 |
|---|---|
| 新增商店物品 | 在 `CONFIG.artifacts` 加条目 + `ITEM_KEYS` 登记 + 商店/背包构建函数加行（老用户商店自动出现新物品） |
| 修改已有功能逻辑 | 直接改代码，无数据迁移 |
| 数据键改名/删除 | 需在代码中保留旧键兼容读取（一次性），或用版本号字段做一次性迁移 |
| 修改商店价格 | 改 `CONFIG.artifacts.<key>.price`（老用户购买状态保留，价格只影响未来购买） |

## 四、版本记录建议

- 每次更新在 commit message 写明改动要点（参照现有 commit 风格）。
- 重大版本可在 `CLAUDE.md` 追加变更记录小节。

## 五、注意

- 纯静态托管无法做服务器端账号：换设备/换浏览器需重新注册（数据随浏览器）。
- 若未来需要跨设备账号/全站统计，需引入后端服务（Node/云函数），届时账号系统与监测系统需重构。
